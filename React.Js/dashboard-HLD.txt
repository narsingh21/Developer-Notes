Design structure for a real-time dashboard

┌──────────────┐
│   WebSocket  │
│   / API      │
└──────┬───────┘
       │
┌──────▼─────────┐
│ Custom Hooks   │  ← data, side effects, cleanup
│ (useLiveData)  │
└──────┬─────────┘
       │
┌──────▼─────────┐
│ Stateful       │  ← state + orchestration
│ Container      │
└──────┬─────────┘
       │ props
┌──────▼─────────┐
│ Structural     │  ← layout only
│ Components     │
└──────┬─────────┘
       │ children
┌──────▼─────────┐
│ Stateless UI   │  ← pure rendering
│ Components     │
└────────────────┘
