--Node.js is open source server Enviroment built on chrome v8 javScript engine for excuting java script code outside browser. 
It provide event drive, non-blocking(asynchronus)  i/o and cross-plateforn run time eviroment for building for building highly scalable cross plateform enviroment to build serverside application in javascript.

---Node.js comes with REPL( Read eval print loop) is a enviroment like unix and cmd. user can intrect with node with commands

---Global objects are built in objects they are part of java script, node.js global objects are available in all modules, they can be use with impoting them.
  example:- buffer, process, global( global namespace -var), console, time function, require method, exports , dirname,

-- modules are logicallu encapsulated functional code, we can import and export them for use. core, local, third party,
 modules are in built in library as  javaScript libraries, A set of function you want to include in your project.



var http.createServer((req,res)=>{
         res.WriteHead(200,{'Content-Type':"application/json"}
         res.write('application is runing on server)
         res.end('you are at server side');
}).listen(8080,()=>{console.log('server is running');})




API: A piece of software can be used by another piece of software, in order to allowed to talk to each other.

Rest API:
Rest is a architecture we build api according to that, rest is based on resources on the resources.We are applying http methods to do crud operations.


Resources 1: http://localhost:8080/api/v1/movies   -get,post. patch, delete, update (http methods on each resources)
Resources 2: http://localhost:8080/api/v1/user  -get,post. patch, delete, update (http methods on each resources)


middleware:

middleare is between req- response cycle. to check or modified the reqs.


router:

// ----------------------------------------------------------------------------------
// app.get('/api/v1/movies', getAllMovies);

// app.get('/api/v1/movies/:id', getMovie);

// app.post('/api/v1/movies', createMovie);

// app.patch('/api/v1/movies/:id', updateMovie);

// app.delete('/api/v1/movies/:id', deleteMovie);

basicaly we have to crate a sperate routers for each resources seprately to split them.(this process called mouting a router in to route)

resource-router = express.Router()  now use them as a middleware
app.use("/api/v1/movies" ,movie-router)
movie-router.route('/').get().post()

server static:

middleware use// express.static('pathname') on top the app.js


Enviroment varibale: 
they are global variables to set the enviroment in which node is running

param Middleware:
params middleware is listen for an id then excute the middleware on this then next.


-----------npm uninstall eslint prettier eslint-config-prettier eslint-plugin-prettier eslint-config-airbnb eslint-plugin-node eslint-plugin-import eslint-plugin-jsx-a11y eslint-plugin-react --save-dev


movieCOntrollerExplained:
step 1---req comes in then first filter req obj... exclude page, sort, limit fields

const queryObj = { ...req.query };
    const excludeFields = ['page', 'sort', 'limit', 'fields'];
    excludeFields.forEach((el) => delete queryObj[el]);

step2-- filtering gte,lte,lt gt operates from query string to $operater beacuse mongo only understand this{'$gte':'50'}
  req.query ={ page: '2', ratingAverage: { gte: '4' } }
     let queryStr = JSON.stringify(queryObj);
    queryStr = queryStr.replace(/\b(lte|lt|gte|gt)\b/g, (match) => `$${match}`);
await MyModel.find({ name: 'john', age: { $gte: 18 } })

step3 now checck is their any sorting their default we set create at this will also give us a query.

step4 now check any fields are their or not the apply select method default exclude __v
    req.query=  { fields: 'name,price,cinemaHall' }
step5-- now we are setting pagination with page limit logic skip values limit method to limiting documents


step6--  finally we excute our query.

  

check module.export lecture
production may cast error kam nii kr rhi hai (koisi bhi error nii kam kr rhi

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

Error handling:

ndb: is a debugger tool which is introduced by the google for debugging our code.


1 way: for error handling in express we have to define a errot handling middleware. this middleware have four arguments  so express will recognise that it a error handling middleware.

//: now we have to create a global error machanism: we had created a AppError class, this class will create error tofor us , now wherer the error is creating we will call error controller there(errro middleware).

//: async function error handling is managed by the catch Async function it takes the function as argument and return a fuction. this function is called by express because in routing we are calling a middleware which is afunction so catchAsync function should retun a function.

//unhandled Rejection like database shutdown should handled to not crash our application. so we have to shout down our application then it will start again by host plateform or some other people.
 process.on('',()=>)

----------------------------------------------------------------------------------------------------------------------------------------------------------

authentication;

Used bcryptjs validator modules for securing password

//;creates asignup route usiing the Jsonweb token to loged in the user.

//create login routes:-
   for password matching as they bcrypted so we use instance method collection doocument .

//protecting movies routes by adding protecting middleware

//autherization according to user roles and permisssion::-- we give only admin to access some routes

//Password reset functionallity::-- Basically we user sends a email to reset password we find the user, then create a token(use crypto) for user in send it to user and save encripted version of tokrn in to daatbase. so  


//sending token via Email::-- using node mailer we had sent the token to the users mail id (create a function using mail id pass the email and token to the sendMail Function);

// reseting password: according to token the user password will reset ** for password reset we are using the password we use save(), not update beacuse we want to run out middleware(password encription we are doing there pre middle) and validation 


----------------------------------------------------------------------------------------------------------------------------------------------------------
1. what is promosify in node.js?
2.what is enviroment for application? (enviroment is context for our application where can had decide variable for each enviroment.)
3.How can we decide ficed type values in mongodb?( By using enum variable andassing  values as an array) 
4.What is an Event Loop in Node.js?
Event loops handle asynchronous callbacks in Node.js. It is the foundation of the non-blocking input/output in Node.js, making it one of the most important environmental features.
5. Differentiate between process.nextTick() and setImmediate()?
6.How would you use a URL module in Node.js?(The URL module in Node.js provides various utilities for URL resolution and parsing. It is a built-in module that helps split up the web address into a readable format.)
7.control flow in Node.js?
8.What is the buffer class in Node.js?(Buffer class stores raw data similar to an array of integers but corresponds to a raw memory allocation outside the V8 heap. Buffer class is used because pure JavaScript is not compatible with binary data
9.What is callback hell?
Callback hell, also known as the pyramid of doom, is the result of intensively nested, unreadable, and unmanageable callbacks, which in turn makes the code harder to read and debug
improper implementation of the asynchronous logic causes callback hell
10.


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Security section:-

//Strongly encrypt user password in to database.
//strongly encrypt password reset token in to database
------------------------------------------------------------
brute force attacks
//use bcrypt to make login request slow
//impliment rate limiting:- limit the no of requests from a single IP
//implement maximum login attempts
-------------------------------------------------------------------
cross side scripting xss attacks
//store JWT in cookie only not in the localStorage
//Sanatize use input data
//
-------------------------------------------------------------
1. what is cookie? cookie is a peice of data which server send to the client. client or browser can't access or modified the cookie they can only sent it or receive.
2. what is rate limiting and how to implement in Node.js? (Its is way of preventing from brut force and deniel service attack. when to many request from a single ip then we can limit it in  to our application.)
3.How to set secure http header in node.js app?( using helmet package it automatically add some secure headers in to our sent response)
4.what is data senitization, why we need it in our application?(data senitization means we are preventing some malicous code to enter into our db from request body.there are two type of attck there 1. noSqlQuery Injection , 2. data sentization against cross-side-Script
5.what is paramter pollution?


------------------------------------------------------------Node + Postgres SQL-------------------------------------------------------------------------




------------------------------------------------------------Advance Node Js--------------------------------------------------

Q.v8?
- v8 projector engine is open source project which is created by google which allow us excute js outside browser.
 - v8 translate or interpret the js code which we write on java script into c++ so

Q. libUV ? c++ library
  - The libuv project over here is a C++ open-source project that gives Node access to the operating systems,underlying filesystem.It gives us access to networking,and it also handles some aspects of concurrency as well.

Q. Thread ?
 - Thread are the sequence of instruction which are going to be executed by cpu core. Deciding which which thread is need to excuting  refed as scedulling. scedulling is controlled by your Operating system. two way to improve it by add more core to machine or operating needs to identify big pause in processing in expensive input and ouput system.
  






------------------------------------mongoose------------------------------------------------

-create Modal 
 const Modal= mongoose.modal('Modalname',schema)

 -three way to execute the query in mongoose to mongo db.
  query.exec((err,result)=>console.log(result))
  query.then(result=>console.log(result))
  const result = await query();

 - Running Query using Modal
    Modal.find({name:'narsingh',city:'noida'},'name address',{skip:5})
    await Modal.findById(id).exec(); // id is equalent _id 
    Modal.findByIdAndDelete(id) // id is equalent _id 
    Modal.findByIdAndUpdate(id,update) // id is equalent _id 

    Modal.findOne({name:'narsingh})
    Modal.findOneAndDelete({name:'narsingh'})

- Aggregation :An aggregation pipeline consists of one or more stages that process documents:Each stage performs an operation on the input documents.
 For example, a stage can filter documents, group documents, and calculate values.

--------------------------------Advance Node---------------------------------------------------------

Event loop?

-Every single time a the event  loop runs its refer to as single tick.


How Node js  Application works in Node Enviroment:----

const pendingTimer =[];
const pendingOsOperation=[];
const pendingLongRunnigOperation=[];

 function NodeProgram() {
...........
.....
.....}


function shouldContinue(){
  checkone: any pending setTimeout, setInterval, setImmediate?
  check two:  any pending OS Task? (like listing on a Port)
  check three: any long running task which is pending.



}


eventLoop(shouldContinue()){

one tick execute body.... 
  1. node look at timer check if any timer is completed excuted that timer. setinterval setTimeout

2. node look at pendingOs operatin or long running operation then execute their call back also.

3. pauser execution and wait for --
 - new pending os task has to done.
- a new pending operation done.
- a timer has to done

4. node look at set immediate

5.Handle any close Event.
   


}


**should continue excutes every time after tick
** when we start an node  program the libuv is create the thread pool automatically.


Q. edit ThreadPool size ? 
process.env.UV_THREADPOOL_SIZE= 5;

Q. can we use threadpools to excutes our js code or only node js function have excess for it?
   We can write custom js that uses the thread pool.


Q.What function in node std library uses the thread pool fun?
--  all fs module, some crytpo func -- also depend on OS  Window or Linux.

Q. How does the threadpool stup firs in to the event loop?
   Task are pending are run by the  thread pool and excutes by  event loop.

Q. Libuv OS Delegation?
  -- Node.js and libuv doesn't do  the Network request. The actual os request is done by the os system. so there is no use of thread pool or eventloop in this scenario.

Q. which of the nodeJS functions are uses OS async Helper feature?
 Almost Network related stuf uses the This OS async Helper.  network request, network response , eventlistner on network?

Q. how the OS async Helpers are relate to Event Loop?
  These task are shown in the Pending OS Task of Application to Event Loop.

  Q. how too increase Node application perfomance?
   - By clustering: make  multiple copy of our  node application.
   - By using thread pool.
-------------------------------------------clusting-------------------------------------------------------------
  ** Blocking the Event Loop ** If there is something which blocks the event loop like some perfomance intensive task( executing a loot of Java script code) then our next works around like db fething, any other task will be blocked.
     for escaping these kind of scenerion in cluster enviroment so  our application get multiple instamce of our application. it have their pro and cons.

Q. Cluster Manager?
   which is responsible for our node clusters health. i it create and manage the cluster worker instances
    cluster module is required for creating clusters
     cluster.fork()

   Q. Do request using OS direct using cmd?
     ab -c 1 -n 1 localhost:3000/

     Q. PM2
      Its a open source cluster management system.
       use application using cluster pm2 start index.js -i 0 , pm set the no of cluster to no logical core of your application.
        pm2 list - health list
        pm2 show index - detail ifno about child
        pm2 monit -  show logs  
        pm2 delete index[application name] - delete the app. or stop all child

        talk about node in cluster mode for dramticallu increasing permonse in concurent havy duty lifing that out application have to do.

        --------------------------------workers pool------------------------------------------------------------------

 webworker-thread is third library.

 ------------------------------------------------------------------------------------------------------
  Q.  how to increase the perfomance of DB quering.
    - using indexing.
    - using some caching server
 Q. Redis 
     redis is inmemory data store. A tyni database which stores in our machine which allow you to read and write data very quickly.


  
